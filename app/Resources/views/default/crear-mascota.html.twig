{% extends 'default/index.html.twig' %}

{% block main %}
    {% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}
    <div class="container">
        <h2>Crear Mascota</h2>
        <p>
            Formulario
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_errors(form) }}
            {{ form_end(form) }}
        </p> 
    </div>
{% endblock %}

